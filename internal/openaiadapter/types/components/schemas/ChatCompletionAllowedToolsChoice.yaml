description: >-
  Constrains the tools available to the model to a pre-defined set.
properties:
  type:
    type: string
    enum:
      - allowed_tools
  allowed_tools:
    $ref: ChatCompletionAllowedTools.yaml
required:
  - type
  - allowed_tools
